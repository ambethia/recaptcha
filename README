== ReCaptcha for Rails

Example implementation of the ReCaptcha plugin in a rails app. Can be installed with rubygems:

  config.gem "recaptcha", :lib => "recaptcha/rails", :source => "http://gemcutter.org"

Or, as a standard rails plugin:

  script/plugin install git://github.com/ambethia/recaptcha.git